{
  "name": "mysql-agent",
  "version": "0.1.0",
  "private": true,
  "description": "AI-native MySQL management tool powered by natural language",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "packageManager": "pnpm@9.0.0"
}
